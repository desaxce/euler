#include <iostream>
#include <stdlib.h>
#include <stdint.h>
#include <cmath>
#include <list>
#include <vector>
#include <deque>
#include <primesieve.hpp>

#define MAX(a, b)	((a) > (b)) ? (a): (b)
#define MIN(a, b)	((a) > (b)) ? (b): (a)
#define ll long long
#define ld long double

using namespace std;


